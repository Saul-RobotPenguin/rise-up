import Map from "../Map";
import { useEffect, useState } from "react";
import axios from "axios";
export default function Meal({ lat, lng }) {
  return (
    <>
      <div>Meal Locations</div>
      {/* <Map lat={lat} lng={lng} locations={locations} /> */}
    </>
  );
}
